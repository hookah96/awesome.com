import React from 'react';
import { position } from './style';

const InformationCard = () => {
  return (
    <div className={position}>
      This will display info for use the website. Remember to use Transition.
    </div>
  );
};

export default InformationCard;
